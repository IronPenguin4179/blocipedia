 <h1>All Wikis</h1>
 <% @wikis.each do |wiki| %>
   <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
           <%= link_to wiki.title, wiki %>
       </h4>
       <h3><%= wiki.user %></h3>
       <h4><%= current_user.id %></h4>
       <h4><%= wiki.private %></h4>
     </div>
   </div>
 <% end %>
